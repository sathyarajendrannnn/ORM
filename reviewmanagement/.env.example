# Backend Environment Variables

# Frontend URLs allowed to access this backend (comma-separated)
# Include common dev ports during development
FRONTEND_URLS=http://localhost:3000,http://localhost:3003,http://localhost:5173

# Database Configuration
DB_USERNAME=root
DB_PASSWORD=root123
